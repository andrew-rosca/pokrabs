# POKRABS Implementation Progress Log

## Project Overview

POKRABS is a web application for hierarchical problem decomposition using the POKRABS framework (Problem → Objective → Key Results → Actions → Blockers → Status). The system recursively breaks down problems until actionable work is identified.

## Architecture Decisions

### Technology Stack
- **Frontend**: React 18+ with TypeScript
- **Backend**: Node.js with Express and TypeScript
- **Database**: SQLite (MVP), with abstraction layer for future PostgreSQL/MySQL support
- **Build Tool**: Vite for frontend (fast HMR, modern tooling)
- **Testing**: Vitest for unit tests, React Testing Library for component tests
- **Containerization**: Docker with single container for MVP

### Project Structure
- **Monorepo approach**: Separate `backend/` and `frontend/` directories
- **Shared types**: TypeScript interfaces/types shared between frontend and backend
- **API-first design**: All business logic implemented in backend API layer
- **Crisp architecture**: Clear separation between UI, API, and persistence layers

### Database Design
- **Repository pattern**: Abstract data access layer for testability and future database support
- **Migration system**: Schema versioning with migration files
- **Soft delete**: All deletions are soft (deletedAt field) to support future undo functionality
- **Hierarchical IDs**: Problems use parent chain IDs (e.g., "a2-df-7f") stored as strings
- **Data isolation**: All queries filter by projectId (and userId in Phase 2)

### ID Generation Strategy
- **LCG-based**: Linear Congruential Generator traverses ID space non-sequentially
- **Globally unique**: Single global counter ensures no duplicates across all projects
- **Non-sequential**: IDs appear random (e.g., i0, i5, na, xf) but deterministic
- **Full coverage**: Every ID in space generated exactly once before repeating
- **Expandable**: Automatically grows to 3-4 characters when space exhausted
- **O(1) generation**: No database lookups needed - just increment counter and apply LCG
- **Start offset**: Begins at middle of space (648/1296) for less predictable first IDs
- **Hierarchical**: Child IDs use same generation; hierarchy maintained via idPath field

### API Design
- **RESTful endpoints**: Standard CRUD operations for problems and projects
- **Special operations**: Dedicated endpoint for "decompose blocker" functionality
- **Error handling**: Consistent error response format
- **Validation**: Input validation on all endpoints
- **User isolation**: Prepare for Phase 2 authentication (user-scoped queries)

### Frontend Architecture
- **Component structure**: Modular, reusable components
- **State management**: React Context API or Zustand for global state
- **Routing**: React Router for deep linking to specific problems
- **Inline editing**: Table cells editable with auto-save on blur/Enter
- **Real-time updates**: Optimistic UI updates with server sync

### Testing Strategy
- **Unit tests**: Business logic, utilities, ID generation
- **Integration tests**: API endpoints, database operations
- **E2E tests**: Full user workflows (create problem, decompose blocker, etc.)
- **Test isolation**: Each test uses ephemeral database/data
- **TDD approach**: Write tests first, then implement functionality

### Development Workflow
- **Hot reload**: Fast development with HMR for both frontend and backend
- **Environment variables**: Separate configs for dev/test/prod
- **Docker support**: Single container for easy deployment
- **Database persistence**: SQLite file mounted as volume in Docker

## Session Log

### 2024-12-31 - Features F001-F006 Implementation

**Completed Features:**
- **F001**: Verified and completed Node.js backend project initialization
  - Backend structure already existed with Express, TypeScript, and proper directory structure
  - Verified package.json, tsconfig.json, and src/ directory structure
  
- **F002**: Verified and completed React frontend project initialization
  - Frontend structure already existed with Vite, React, TypeScript
  - Verified package.json, tsconfig.json, vite.config.ts, and src/ directory structure
  - React Router already configured
  
- **F003**: Verified shared TypeScript types setup
  - Shared types already exist in `shared/types.ts`
  - Includes Problem, Project, Status enum, and request interfaces
  - Types are properly structured for both frontend and backend use
  
- **F004**: Configured environment variables and .env.example files
  - Created `backend/.env.example` with DATABASE_TYPE, DATABASE_URL, PORT, NODE_ENV, TEST_MODE
  - Created `frontend/.env.example` with VITE_API_URL
  - dotenv already configured in backend
  
- **F005**: Set up SQLite database connection and client
  - Installed `better-sqlite3` and `@types/better-sqlite3`
  - Created `backend/src/database/client.ts` with:
    - Database client singleton with dynamic configuration
    - Connection string checking and client recreation
    - Support for test mode (aggressive connection checking)
    - Automatic directory creation for SQLite database files
    - Foreign key support enabled
  
- **F006**: Created database migration system
  - Created `backend/src/database/migrate.ts` with:
    - Migration version tracking in `schema_migrations` table
    - Migration file loading and parsing (supports up/down migrations)
    - Transaction-based migration execution
    - Rollback functionality
  - Created `backend/src/database/setup.ts` to initialize database and run migrations
  - Created sample migration `001_initial.sql` to test the system
  - Verified migration system works correctly (migrations run, version tracking works)

**Implementation Details:**
- Database client follows the pattern from coder instructions: lazy creation, connection string checking, client recreation
- Migration system supports both running from project root and backend directory
- Migration files use format: `{version}_{name}.sql` with optional `-- DOWN` section for rollbacks
- All features tested and verified to work correctly

**Next Steps:**
- Continue with F007 (Create Projects table migration) and F008 (Create Problems table migration)

### 2024-12-31 - LCG-Based ID Generator Implementation

**Completed:**
- **F011 (Updated)**: Implemented LCG-based ID generator
  - Replaced random ID generation with Linear Congruential Generator (LCG)
  - Uses formula: `next = (value * 37 + 5) mod space_size` for full period coverage
  - Global counter stored in `IdCounter` table (singleton pattern)
  - IDs start from middle of space (offset 648/1296) for less predictable first IDs
  - Automatic expansion to 3-char when 2-char space (1,296 IDs) is exhausted
  - All 1,296 two-character IDs generated exactly once before expansion
  - O(1) generation - no database uniqueness checks needed

**Database Changes:**
- Added `IdCounter` model to Prisma schema
- Migration: `20251231202955_add_id_counter` creates `id_counter` table
- Table stores: `id` (singleton key "global"), `counter` (current value), `length` (current ID length)

**Code Changes:**
- Created `backend/src/utils/id-generator.ts` with:
  - `generateProblemId()` - main ID generation function
  - `lcgNth()` - optimized LCG calculation with modular exponentiation for large n
  - `numberToBase36()` - converts numbers to base36 strings
  - `calculateSpaceSize()` - computes ID space size
  - `getIdCounterState()` - monitoring/debugging helper
  - `resetIdCounter()` - test utility
- Removed `generateChildProblemId()` - redundant wrapper, use `generateProblemId()` directly
- Removed `backend/src/utils/id-checker.ts` and tests - obsolete (LCG guarantees uniqueness)

**Testing:**
- 23 tests covering LCG math, ID generation, space expansion, full 2-char space uniqueness
- All tests pass (38 total across all utils)

**Technical Details:**
- LCG parameters: multiplier=37, increment=5 (satisfies full period requirements for base36)
- Initial offset: 648 (middle of 1,296 space) for less predictable first IDs
- Space sizes: 2-char=1,296, 3-char=46,656, 4-char=1,679,616
- Prisma 6 used (downgraded from 7 due to adapter configuration issues)

**Next Steps:**
- Continue with F007 (Create Projects table migration) and F008 (Create Problems table migration)


### 2024-12-31 - Database Migrations and Test Framework Setup

**Completed:**
- **F007**: Projects table migration
  - Migration already existed in `20251231201033_init/migration.sql`
  - Creates `projects` table with: id (TEXT PRIMARY KEY), name (TEXT), createdAt (DATETIME), deletedAt (DATETIME nullable)
  - Includes index on `deletedAt` for soft delete queries
  - Verified schema matches Prisma model definition

- **F008**: Problems table migration
  - Migration already existed in `20251231201033_init/migration.sql`
  - Creates `problems` table with all required fields:
    - Core: id, idPath, problem, objective, keyResults, actions, blockers
    - Metadata: status (enum), votes, priority, labels
    - Relationships: parentId (nullable), projectId (required)
    - Timestamps: createdAt, updatedAt, deletedAt
  - Foreign keys: parentId → problems(id) RESTRICT, projectId → projects(id) CASCADE
  - Indexes: projectId, parentId, deletedAt, projectId+deletedAt, status, priority, votes
  - Verified schema matches Prisma model definition

- **F010**: Test framework setup
  - Backend: Vitest already configured with test scripts
  - Frontend: Installed React Testing Library, @testing-library/jest-dom, @testing-library/user-event, jsdom
  - Created `frontend/vitest.config.ts` with jsdom environment
  - Created `frontend/src/test-setup.ts` for test configuration
  - Created sample test `frontend/src/App.test.tsx` to verify setup
  - All tests passing: backend (38 tests), frontend (1 test)

**Implementation Details:**
- Migrations use Prisma Migrate for database-agnostic schema management
- All migrations verified to match Prisma schema definitions
- Test framework ready for both unit and component testing
- Frontend test setup includes jsdom for DOM simulation

**Next Steps:**
- Continue with API implementation (F015-F020: Models and API endpoints)

### 2024-12-31 - Repository Implementation and Test Isolation

**Completed:**
- **F015 & F016**: Updated shared types
  - Added `deletedAt` fields to Project and Problem interfaces
  - Types already existed in `shared/types.ts`, just needed to add soft delete support

- **F017**: Project Repository Implementation
  - Created `IProjectRepository` interface with CRUD methods
  - Implemented `PrismaProjectRepository` using Prisma Client
  - Methods: create, findById, findAll, update, softDelete
  - All queries automatically exclude soft-deleted records (deletedAt IS NULL)
  - 12 comprehensive tests, all passing

- **F018**: Problem Repository Implementation
  - Created `IProblemRepository` interface with CRUD methods
  - Implemented `PrismaProblemRepository` using Prisma Client
  - Methods: create, findById, findByProjectId, findByParentId, update, softDelete, checkIdExists
  - Integrates with LCG ID generator for automatic ID generation
  - Computes idPath for hierarchical problems
  - Maps Prisma enums to shared Status enum
  - 19 comprehensive tests, all passing

- **F019**: Repository Factory Pattern
  - Created `repository-factory.ts` with `getProjectRepository()` and `getProblemRepository()`
  - Supports dependency injection for testing (can pass Prisma client)
  - Singleton pattern for production, new instances for testing
  - Includes `resetRepositories()` utility for test cleanup

- **F020**: Soft Delete Functionality
  - All delete operations set `deletedAt` timestamp instead of removing records
  - All find/query methods automatically filter `deletedAt IS NULL`
  - Tested in both Project and Problem repositories
  - No hard deletes - all deletions are soft

**Test Isolation Solution:**
- Created `backend/src/test-helpers/database.ts` with isolated database utilities
- Each test file gets its own unique database file
- `setupTestDatabase()` creates isolated database and sets `DATABASE_URL` env var
- `cleanupTestDatabase()` removes database files after tests
- Updated all repository tests to use isolated databases
- Updated ID generator tests to use isolated databases
- All 69 tests now pass when run together (previously had interference issues)

**Files Created:**
- `backend/src/models/project-repository.ts` - Interface
- `backend/src/models/prisma-project-repository.ts` - Implementation
- `backend/src/models/problem-repository.ts` - Interface
- `backend/src/models/prisma-problem-repository.ts` - Implementation
- `backend/src/models/repository-factory.ts` - Factory pattern
- `backend/src/models/project-repository.test.ts` - 12 tests
- `backend/src/models/problem-repository.test.ts` - 19 tests
- `backend/src/test-helpers/database.ts` - Test isolation utilities
- `backend/src/test-helpers/README.md` - Usage documentation

**Technical Details:**
- Repositories use Prisma Client for database-agnostic operations
- Automatic enum mapping between Prisma and shared types
- ID generation integrated with LCG-based generator
- idPath computation for hierarchical problems
- Complete test coverage with isolated databases

**Next Steps:**
- Continue with API endpoints (F021-F025: REST API implementation)

### 2024-12-31 - API Endpoints Implementation (F021-F029)

**Completed:**
- **F021**: Express Server Setup
  - Enhanced server with comprehensive error handling middleware
  - Prisma error handling (404 for not found, 409 for conflicts)
  - 404 handler for unknown routes
  - Health check endpoint at `/health`
  - CORS and JSON body parser middleware configured

- **F022**: GET /api/projects endpoint
  - Lists all non-deleted projects
  - Returns JSON array of projects
  - Automatically excludes soft-deleted records
  - Error handling for database failures

- **F023**: POST /api/projects endpoint
  - Creates new project with UUID generation
  - Input validation (name required, non-empty)
  - Trims whitespace from project name
  - Returns 201 with created project

- **F024**: GET /api/projects/:id endpoint
  - Retrieves project by ID
  - Returns 404 if project not found or soft-deleted
  - Error handling for invalid IDs

- **F025**: GET /api/projects/:projectId/problems endpoint
  - Lists all problems for a project
  - Verifies project exists before querying problems
  - Excludes soft-deleted problems
  - Returns 404 if project not found

- **F026**: POST /api/projects/:projectId/problems endpoint
  - Creates new problem in a project
  - Validates required fields (problem, objective)
  - Validates status enum values
  - Validates parentId (if provided) exists and belongs to same project
  - Generates unique ID using LCG generator
  - Sets default values (status: NotStarted, votes: 0, priority: 0)
  - Computes idPath for hierarchical problems
  - Returns 201 with created problem

- **F027**: GET /api/problems/:id endpoint
  - Retrieves problem by ID
  - Returns 404 if problem not found or soft-deleted
  - Error handling for invalid IDs

- **F028**: PATCH /api/problems/:id endpoint
  - Updates problem with partial updates
  - Validates status enum values
  - Validates votes and priority are numbers
  - Updates only provided fields
  - Automatically updates updatedAt timestamp
  - Returns 404 if problem not found

- **F029**: DELETE /api/problems/:id endpoint
  - Soft deletes problem (sets deletedAt timestamp)
  - Returns 404 if problem not found
  - Returns 204 No Content on success
  - Deleted problems excluded from all list queries

**Implementation Details:**
- Routes support dependency injection for testing via Prisma client attached to request
- All endpoints use repository pattern for data access
- Comprehensive input validation on all endpoints
- Consistent error response format
- Soft delete exclusion in all queries
- Proper HTTP status codes (200, 201, 204, 400, 404, 409, 500)

**Testing:**
- 30 comprehensive API tests, all passing
- Tests use isolated databases via middleware
- Covers success cases, validation errors, 404s, and soft delete behavior
- Test files: `backend/src/api/projects.test.ts` (21 tests), `backend/src/api/problems.test.ts` (9 tests)

**Files Created:**
- `backend/src/api/projects.ts` - Project routes (GET, POST, project-specific problem routes)
- `backend/src/api/problems.ts` - Problem routes (GET, PATCH, DELETE)
- `backend/src/api/projects.test.ts` - Project API tests
- `backend/src/api/problems.test.ts` - Problem API tests

**Dependencies Added:**
- `uuid` and `@types/uuid` for project ID generation
- `supertest` and `@types/supertest` for API testing

**Technical Details:**
- Routes check for Prisma client in request object (for testing) or use default
- TypeScript types extended to include optional Prisma client on Express Request
- Error middleware handles Prisma-specific errors (P2025, P2002)
- Route ordering: specific routes (/:projectId/problems) before generic routes (/:id)

**Next Steps:**
- Frontend implementation to consume API endpoints
- Optional: F030 (decompose endpoint) if needed

### 2024-12-31 - MVP Frontend Implementation and UI Enhancements

**Completed:**
- **Database Seeding (F070-F071)**: 
  - Created seed script with Default project and sample problem chain
  - Removed hardcoded IDs - now uses LCG-based auto-generation
  - Seed script deletes existing Default project before reseeding
  - Integrated into database setup workflow

- **F037**: React Router Setup
  - React Router already configured in App.tsx
  - Routes for home page and problem detail page
  - Deep linking support via /:problemId route

- **F038**: API Service Layer
  - Created `frontend/src/services/api.ts` with fetch-based API client
  - Functions for all API endpoints (projects, problems)
  - Error handling and response parsing
  - Uses VITE_API_URL environment variable

- **F040**: Main Table Component Structure
  - Created `ProblemsList` component
  - Table with columns: ID, Problem, Objective, Status, Votes
  - Displays problems in table rows
  - Loading, error, and empty states

- **F041**: Visual Hierarchy with Indentation
  - Calculates depth from idPath (counts dashes)
  - Applies indentation styling: 24px per depth level
  - Root problems: no indent, children: 1 level, etc.
  - Hierarchical sorting by idPath ensures parent-child order

- **F046**: Color Coding for Status
  - Status badges with color coding
  - Blocked = red, In Progress = cyan/blue, Resolved = green, Not Started = gray
  - Colors adapt to light/dark mode
  - Visual status indicators

**UI Enhancements:**
- **Light/Dark Mode Toggle**
  - Theme toggle button in top-right corner
  - Persists preference in localStorage
  - Respects system preference on first load
  - Smooth theme transitions
  - Comprehensive CSS variables for theming

- **Enhanced Styling**
  - Modern, clean design with improved typography
  - Responsive layout with max-width container
  - Sticky header with shadow
  - Hover effects on table rows
  - Professional color scheme
  - Smooth transitions and animations

- **Hierarchical Sorting**
  - Problems sorted by idPath for natural hierarchical order
  - Ensures parent problems appear before children
  - Maintains tree structure visually

**Files Created:**
- `backend/src/database/seed.ts` - Database seeding script
- `frontend/src/services/api.ts` - API service layer
- `frontend/src/components/ProblemsList.tsx` - Problems table component
- `frontend/src/components/ThemeToggle.tsx` - Theme toggle component
- `frontend/src/index.css` - Comprehensive styling with light/dark mode

**Technical Details:**
- Seed script uses repository pattern for ID generation
- Frontend sorts problems by idPath for hierarchical display
- Theme system uses CSS variables for easy customization
- All styling is responsive and mobile-friendly

**Next Steps:**
- Summary/detail view toggle (F043)
- Status dropdown (F044)
- Vote increment button (F045)

---

## Session: Inline Editing Implementation and Comprehensive Testing (Latest)

**Date**: Current Session

**Completed Features:**
- F042: Inline editing for table cells

**Implementation Details:**

### Inline Editing Component (EditableCell)
- **Overlay-based editing**: Input/textarea appears as fixed overlay above table
- **Comfortable editing**: Overlay can expand beyond cell boundaries for better UX
- **Single-line and multiline support**: Input for simple fields, textarea for arrays
- **Keyboard shortcuts**: 
  - Enter to save (single-line)
  - Cmd/Ctrl+Enter to save (multiline)
  - Escape to cancel
- **Auto-save on blur**: Changes saved when clicking outside
- **Optimistic updates**: UI updates immediately, reverts on error
- **Error handling**: Graceful error handling with user feedback

### Enhanced ProblemsList
- **All fields editable**: Problem, Objective, Key Results, Actions, Blockers
- **JSON field handling**: Properly parses and formats JSON structures
- **Array field editing**: Multiline editing for keyResults, actions, blockers
- **Summary/detail structure**: Supports JSON objects with summary/detail fields
- **Full-width table**: Spreadsheet-like appearance with compact styling

### Comprehensive Test Coverage

**Backend API Tests (106 tests passing):**
- Enhanced PATCH /api/problems/:id endpoint tests
- Tests for updating all fields: problem, objective, keyResults, actions, blockers
- Tests for updating multiple fields simultaneously
- Validation tests for status, votes, priority
- Error handling tests for non-existent problems
- All existing repository and API tests maintained

**Frontend UI Tests (29 tests passing):**
- **EditableCell Component Tests (12 tests)**:
  - Rendering and placeholder display
  - Entering edit mode on click
  - Saving on blur and Enter key
  - Canceling with Escape
  - Multiline editing with Cmd/Ctrl+Enter
  - Error handling and reversion
  - Backdrop click to close
  - Text selection on edit

- **ProblemsList Component Tests (15 tests)**:
  - Loading, error, and empty states
  - Displaying problems hierarchically
  - Status badges and votes display
  - Editing all field types (problem, objective, keyResults, actions, blockers)
  - Error handling and reversion
  - Hierarchical sorting verification

**Test Infrastructure:**
- Mocked localStorage for theme preference testing
- Mocked window.matchMedia for theme detection
- Proper test isolation with per-test-file databases
- Async handling with waitFor and act utilities
- getBoundingClientRect mocking for overlay positioning

**Files Created:**
- `frontend/src/components/EditableCell.tsx` - Reusable inline editing component
- `frontend/src/components/EditableCell.test.tsx` - Comprehensive component tests
- `frontend/src/components/ProblemsList.test.tsx` - Integration tests for inline editing
- `backend/src/api/problems.test.ts` - Enhanced API tests (updated)

**Files Modified:**
- `frontend/src/components/ProblemsList.tsx` - Integrated EditableCell for all editable fields
- `frontend/src/index.css` - Updated styles for overlay editing
- `frontend/src/test-setup.ts` - Added mocks for localStorage and matchMedia
- `backend/src/api/problems.test.ts` - Added comprehensive field update tests

**Technical Highlights:**
- Overlay positioning using getBoundingClientRect for precise placement
- Fixed positioning allows overlay to expand beyond cell boundaries
- Proper React state management with useEffect for cellRect calculation
- Optimistic UI updates for responsive feel
- Error recovery with automatic reversion
- Full test coverage ensures reliability

---

## Session: Status Dropdown and Vote Increment Implementation (Latest)

**Date**: Current Session

**Completed Features:**
- F044: Status dropdown in table
- F045: Vote increment button
- F046: Color coding for status (already implemented, now marked complete)

**Implementation Details:**

### Status Dropdown (F044)
- **Replaced static badges**: Status badges replaced with interactive dropdown selects
- **All status options**: Dropdown includes Not Started, In Progress, Blocked, Resolved
- **Color-coded styling**: Dropdown maintains status color coding from badges
- **Optimistic updates**: UI updates immediately on selection change
- **Error handling**: Reverts to original status on API failure
- **Accessible**: Proper select element with keyboard navigation support
- **Hover effects**: Visual feedback on hover and focus states

### Vote Increment Button (F045)
- **Clickable votes**: Vote count displayed as clickable button
- **One-click increment**: Single click increments vote count by 1
- **Optimistic updates**: UI updates immediately before API confirmation
- **Error handling**: Reverts to original vote count on API failure
- **Visual feedback**: Hover effects indicate clickability
- **Tooltip**: Title attribute provides user guidance

### Test Coverage

**Frontend UI Tests (20 tests passing):**
- **Status Dropdown Tests (3 tests)**:
  - Display status dropdowns with correct values
  - Change status via dropdown selection
  - Handle status change errors gracefully

- **Vote Increment Tests (2 tests)**:
  - Increment votes on button click
  - Handle vote increment errors gracefully

- All existing tests maintained and passing

**Backend Tests (106 tests passing):**
- All existing API tests maintained
- Status and votes update endpoints already tested

**Files Modified:**
- `frontend/src/components/ProblemsList.tsx` - Added status dropdown and vote increment handlers
- `frontend/src/index.css` - Added styling for status-select dropdown
- `frontend/src/components/ProblemsList.test.tsx` - Added tests for status and vote features

**Technical Highlights:**
- Status dropdown uses native select element for accessibility
- Color coding maintained through CSS classes on select element
- Optimistic UI updates for responsive user experience
- Error recovery with automatic reversion
- Comprehensive test coverage ensures reliability

---

## Session: Fix Status Formatting in Dropdown (Latest)

**Date**: Current Session

**Issue Fixed:**
- Status dropdown lost color formatting after implementation

**Root Cause:**
- Inline styles (`backgroundColor: 'transparent'` and `color: 'inherit'`) were overriding CSS classes
- CSS classes for status colors were defined but not applying due to style specificity

**Solution:**
- Removed conflicting inline styles from status select element
- Kept only width-related inline styles that don't interfere with colors
- Status dropdown now properly displays color-coded backgrounds and text

**Files Modified:**
- `frontend/src/components/ProblemsList.tsx` - Removed conflicting inline styles from status select

---

## Session: Create New Problem Feature (F061) Implementation

**Date**: Current Session

**Completed Features:**
- F061: Create new problem functionality

**Implementation Details:**

### Row Handle and Actions Panel
- **Row handle**: Added drag handle (⋮⋮ icon) that appears on hover for future drag-and-drop functionality
- **Actions panel**: Panel slides in from left on row hover, containing action buttons
- **Always visible bottom row**: Bottom row with + button always visible for creating top-level problems
- **Hover interactions**: Handle and actions panel appear smoothly on row hover

### Create Problem Functionality
- **API service**: Added `createProblem` function to API service layer
- **Handler logic**: 
  - Clicking + on a problem row creates a child of that problem
  - Clicking + on bottom row creates a top-level problem (no parent)
- **Default values**: New problems created with default JSON structure for all fields
- **Auto-refresh**: Problems list automatically refreshes after creation to show new problem with correct idPath

### UI/UX Improvements
- **Handle size**: 24px to match button size for better visibility
- **Button positioning**: Buttons positioned at left edge of table (intersection of row border and table vertical border)
- **Smooth animations**: Panel slides in with smooth transitions
- **Error handling**: Improved error messages showing actual backend errors

### Bug Fixes
- **Status validation**: Fixed backend validation to accept 'Actionable' status value
- **Database migration**: Created fix script to update old 'Not Started' values to 'Actionable' in database
- **Test coverage**: Added comprehensive UI tests for create problem functionality

### Files Created/Modified:
- `frontend/src/services/api.ts` - Added createProblem function
- `frontend/src/components/ProblemsList.tsx` - Added row handle, actions panel, and create handler
- `frontend/src/index.css` - Added styles for row handle and actions panel
- `frontend/src/components/ProblemsList.test.tsx` - Added create problem tests
- `backend/src/api/projects.ts` - Improved status validation
- `backend/src/database/fix-status-values.ts` - Script to fix old status values
- `backend/src/api/projects.test.ts` - Added test for JSON stringified fields

**Technical Details:**
- Row handle uses `cursor: grab` for future drag-and-drop
- Actions panel uses CSS transforms for smooth slide-in animation
- Create handler determines parentId based on which row's button was clicked
- Status enum correctly serializes to 'Actionable' string value
- All tests passing: Backend (23), Frontend (36)

**Next Steps:**
- Summary/detail view toggle (F043)
- Delete problem functionality (F062)

### 2026-01-01 - F029 Soft Delete Endpoint Stabilization

**Completed:**
- F029 verified and marked complete for DELETE `/api/problems/:id`
- Updated test expectation to allow detailed invalid status messages
- Switched backend/frontend `npm test` scripts to `vitest run` to avoid interactive watch mode

**Testing:**
- Backend: 109 tests passing

**Notes:**
- Kept detailed invalid-status error messaging in API responses

### 2026-01-01 - F062 Delete Problem UI

**Completed:**
- F062 implemented: delete button added to hover actions panel beside +
- Added `deleteProblem` API client, confirmation prompt, and optimistic delete handler with refresh
- Styled delete action with danger hover state
- Prevented unhandled rejection noise by logging instead of rethrowing in UI handlers

**Testing:**
- Frontend: 38 tests passing (`npm test`)

**Notes:**
- Delete action refetches problems after removal to keep hierarchy consistent

### 2026-01-01 - Cascade Soft Delete for Problem Hierarchy

**Completed:**
- Implemented cascading soft delete in `PrismaProblemRepository.softDelete` (parent + descendants via `idPath` prefix)
- Added API test to verify children are soft-deleted when a parent is deleted

**Testing:**
- Backend: 110 tests passing (`npm test`)

### 2026-01-01 - List Editor Prototype (Actions/Blockers)

**Completed:**
- Built `ListEditor` component prototype for list-style fields (actions/blockers) with add/edit/delete/reorder controls and overlay/backdrop handling
- Added isolated tests to verify rendering, add/remove, reordering, trimming on save, and cancel behavior

**Files Added:**
- `frontend/src/components/ListEditor.tsx`
- `frontend/src/components/ListEditor.test.tsx`

**Testing:**
- Frontend: 48 tests passing (`npm test` in `frontend/`)

### 2026-01-02 - Shared DeleteButton Component

**Completed:**
- Extracted reusable `DeleteButton` component from inline delete implementations
- Shared between `ProblemsList` (main table) and `ListEditor`
- Ensures consistent delete behavior across all delete buttons:
  - Click shows confirmation dialog (Delete? ✔/✕)
  - Ctrl+click deletes immediately without confirmation
  - Escape key cancels confirmation
  - Red button on hover
- Supports configurable size (small for ListEditor, default for main table)
- Supports custom aria-label and title props

**Files Added:**
- `frontend/src/components/DeleteButton.tsx` - Reusable delete button with confirmation
- `frontend/src/components/DeleteButton.test.tsx` - Tests (11 tests)

**Files Modified:**
- `frontend/src/components/ListEditor.tsx` - Uses DeleteButton for row delete
- `frontend/src/components/ProblemsList.tsx` - Uses DeleteButton for problem delete

**Testing:**
- Frontend: 61 tests passing (`npm test` in `frontend/`)

### 2026-01-02 - ListEditor Component Complete

**Completed:**
- Built `ListEditor` component for editing list fields (actions/blockers)
- Uses `contentEditable` div to avoid height changes between display/edit modes
- Features:
  - Add items via blank row at bottom with placeholder text
  - Edit items inline with click-to-edit and select-all on entry
  - Delete items with confirmation dialog (Delete? ✔/✕)
  - Ctrl+click to delete without confirmation (works on Mac via onContextMenu)
  - Escape cancels delete confirmation (or closes editor if no confirmation)
  - Drag-and-drop reordering via handle
  - Automatic URL linkification in display mode
  - Compact row height with 4px vertical padding
  - Delete button turns red on hover (matches main table styling)
- Removed demo route from App.tsx (was used for development)

**Files Modified:**
- `frontend/src/components/ListEditor.tsx` - Main component
- `frontend/src/components/ListEditor.test.tsx` - Tests (8 tests)
- `frontend/src/index.css` - Added placeholder CSS rule
- `frontend/src/App.tsx` - Removed demo route

**Testing:**
- Frontend: 50 tests passing (`npm test` in `frontend/`)

### 2026-01-01 - Mark Feature Flags F038 & F041 Complete

**Completed:**
- Marked F038 (Frontend API service layer) as passing in `feature_list.json`
- Marked F041 (Hierarchy indentation) as passing in `feature_list.json`

**Notes:**
- No code changes required; features already implemented in `frontend/src/services/api.ts` and `frontend/src/components/ProblemsList.tsx`
- No additional tests were run in this update (previous backend suite already passing)
